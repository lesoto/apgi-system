# Build-time dependencies for creating executables
# These are only needed during the build process, not at runtime

# Windows executable builder
pyinstaller>=6.15.0

# Windows-specific (only install on Windows)
pywin32==306; sys_platform == "win32"

# macOS executable builder (only install on macOS)
py2app>=0.28.7; sys_platform == "darwin"

# Icon conversion and manipulation
Pillow>=10.1.0

# Build utilities
setuptools>=69.0.0
wheel
